# Progress

## Current State

- Project folder created.
- Base workspace structure created.
- NVDLA RTL cloned into `rtl/nvdla` at revision `8e06b1b`.
- Reference `partition_m` and `partition_a` SDC files copied into `constraints/`.
- Initial filelists, library setup, and DC elaborate smoke-test script added.

## Next Step

- Run `partition_m` DC analyze/elaborate/link smoke test and resolve compile/link issues.
